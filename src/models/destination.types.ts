export type ImagesTypes = {
  png: string;
  webp: string;
};

export type DestinationsTypes = {
  name: string;
  images: ImagesTypes;
  description: string;
  distance: string;
  travel: string;
};
